spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  
  flyway:
    enabled: false

app:
  jwt:
    secret: test_secret_key_minimum_32_characters_for_security_testing
    expiration: 86400000
